<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="titleStyle" parent="android:Widget.Holo.ActionBar">
        <item name="android:background">@drawable/red</item>
        <item name="android:titleTextStyle">@style/titleStyle_text</item>
        <item name="android:displayOptions">showTitle</item>
    </style>
    <style name="titleStyle_text">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:gravity">center</item>

    </style>
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/titleStyle</item>
    </style>

</resources>
